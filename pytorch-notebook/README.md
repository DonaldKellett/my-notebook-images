# `pytorch-notebook`

PyTorch notebook with useful libraries and extensions

## Building the image locally

```bash
podman build -t pytorch-notebook .
```

## Using

See the [Jupyter Docker Stacks](https://jupyter-docker-stacks.readthedocs.io/en/latest/) documentation for details
